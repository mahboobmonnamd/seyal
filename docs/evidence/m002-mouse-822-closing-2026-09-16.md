# M002 #822 current-head closing evidence — 2026-09-16

| Field | Value |
| --- | --- |
| Source head | `2444c78` (`origin/master`) |
| Host | Darwin arm64 / macOS 26.5.2 / Rust 1.98.0 |

Application mouse reporting and Shift host-override are already on master. This record is the current-head evidence package for independent review.

## Portable mouse

```text
m002_mouse 10/10
  mouse_modes_are_canonical_mutually_exclusive_and_queryable
  sgr_and_x10_reports_follow_canonical_mode
  shift_press_latches_host_and_suppresses_report
  after_release_unshifted_press_resumes_application_report
  host_latched_move_ignores_reporting_any
  reporting_off_press_is_host_without_shift
  unshifted_move_release_while_latched_stay_host_no_report
  shift_wheel_is_host_noop_no_selection_change
  wheel_encodes_sgr_buttons
  drag_and_any_motion_matrix
```

`testXtermButtonMapDropsButtonsBeyondRight` passed in `SeyalHostComponentTests` 18/18.

## Headed Flow/Blocks

From the same exclusive-Runtime `scripts/test-macos-ui.sh` run:

```text
testHostMouseClickStaysOnFlowBlocks   passed (16.262s)
```

A host click on `terminal-input` while Flow is eligible requests composer focus and does not crash. Application SGR/X10 reports are Raw/TUI-only (same eligibility gate as keyboard). Headed htop/Neovim/Shift-override is the reviewer matrix below.

## Reviewer verification (Raw/TUI)

1. `htop` (or another mouse-aware ncurses app): click controls, scroll lists.
2. Neovim with mouse enabled: click to move cursor, wheel-scroll.
3. Hold Shift, drag-select; the TUI must not receive the same event.
4. Release override; application mouse resumes immediately.
5. Resize / move between displays; click known cells; coordinates stay correct.
6. Enter/leave alternate screen; host vs app ownership follows canonical modes.

Documentation impact: N/A for User Guide (no new setting). Terminfo already advertises implemented `kmous`/`XM`/`xm`. This file is developer evidence only.
