#!/usr/bin/env bash
set -euo pipefail

input="${1:?retained fuzz seed path is required}"
SEYAL_FUZZ_INPUT="$input" cargo test --quiet -p seyal-protocol --test pass7_fuzz_smoke pass7_protocol_decode_seed -- --ignored --exact
