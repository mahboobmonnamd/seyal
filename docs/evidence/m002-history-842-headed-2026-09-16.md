# M002 #842 headed HistoryStore / reflow — 2026-09-16

| Field | Value |
| --- | --- |
| Branch | `issue/842` from `master` `1aa1a7c` |
| Relationship | `Refs #842` — not `Closes` |
| Host | Darwin arm64 / macOS 26.5 / Rust 1.98 |

## Scope of this pickup

Gate 3 only: headed user-visible history/reflow on the ADR-015 thin host.
No HistoryStore harness changes. No #673 five-cohort promotion. Keep #819
and #842 open.

## Added headed cases

`SeyalHostHistoryUITests` (separate file so it does not collide with #823):

1. `testLongOutputAndResizeStayOnFlowBlocks` — print numbered ASCII + CJK +
   emoji, resize narrow then wide, keep Flow/Blocks, retain screenshots.
2. `testAlternateScreenExitRestoresFlowHistorySurface` — enter/leave
   alternate screen, composer/transcript must return so TUI frames do not
   replace primary Flow history.

Metal does not expose PTY bytes as AX text. These cases prove the host
stays on the production Flow surface across scrollback-scale output,
resize, and alt-screen return. They do not replace grapheme-atomic visual
inspection of every reflowed cell.

Focused headed result on this host after rebase onto `1aa1a7c`:

```text
** TEST EXECUTE SUCCEEDED **
SeyalHostHistoryUITests 2/2
Executed 2 tests, with 0 failures in 58.610s
```

## Still open

1. Accepted ARM64 physical/RSS matrix vs #673/#818 (deferred with the owner
   measurement pass).
2. Allocation/resource instrumentation on the five-cohort runner.
3. Independent exact-head review after those gates.

Documentation impact: N/A for User Guide (no new setting). Developer evidence
only.
