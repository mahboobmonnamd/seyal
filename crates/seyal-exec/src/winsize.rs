//! PTY window-size value/operation boundary.
//!
//! Public size values and resize operations are introduced together with the
//! child-visible resize tests so the scaffold does not invent an unused API.
