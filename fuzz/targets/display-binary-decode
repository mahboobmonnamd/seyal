#!/usr/bin/env bash
set -euo pipefail

input="${1:?retained fuzz seed path is required}"
SEYAL_FUZZ_INPUT="$input" cargo test --quiet -p seyal-runtime --test display_fuzz_smoke display_binary_decode_seed -- --ignored --exact