# M001.1 thin-host qualification

This note records what the rebuilt `Seyal.app` on `remove-all-swift` actually
proves. It is not headed permission to close #878.

## Proven on the new host

- Production launch path is `ProductChromeHostView` over `seyal_app_*` snapshots.
  There is no `SeyalShellView` / `SeyalShellState` source.
- Native widgets dispatch typed Rust actions and rebuild from borrowed snapshots.
- Metal, IME (`NSTextInputClient` + composer `NSTextView` bridge), VoiceOver
  posting, pasteboard, and bundled-helper launch remain native.
- Mechanical gates:
  - `scripts/check-host-product-fixtures.py`
  - `scripts/check-thin-swift-boundary.py` (positive host files + controlled
    `enum InspectorMode` negative fixture)
- Component XCTest consumes `seyal_app_create` / shell / chrome / composer
  projections only.
- Launch XCUI exists for chrome, inspector, workspace row, recovery, and the
  Metal pane identifiers.

## Not claimed

Do not treat old XCUI, Foundation display-link-off benches, or the deleted
preview host as headed proof.

Still required before #885 / #878 Done:

- Workspace/Tab/Pane split/focus/close on a multi-pane tree (M001 production
  shell remains one Workspace / one Tab / one Pane and fail-closes extras)
- Agent/attention navigation against live activity rows
- Flow/Raw/TUI + composer submit/busy/stale on a live PTY
- IME/dead keys beyond the NSTextInputClient + composer Enter bridge
- Cmd+C/V/Q, Vim/htop/alt-screen, detach/reopen
- CPU/RSS/latency/GPU versus an accepted pre-migration headed baseline
- Independent architecture/performance/security review
