# M001.1 thin-host qualification

This note records what the rebuilt `Seyal.app` on `remove-all-swift` proves for
#885. It does not cite old XCUI or Foundation display-link-off benches.

## Headed Usable Terminal Gate (this host)

Local headed `make ui-test` on Apple M5 Pro, macOS 26.5.2, arm64, Debug
`Seyal.app` with the bundled `seyal-runtime` helper:

| Case | Result |
|---|---|
| Launch one window / one pane / chrome / inspector / recovery / terminal | PASS |
| Left-panel Workspaces ↔ Tabs (Rust action projection) | PASS |
| Composer or terminal type + Enter (`echo seyal-usable-gate`) | PASS |
| Cmd+C / Cmd+V remain in-process; Cmd+Q terminates | PASS |
| Component XCTest: ABI + no `SeyalShell*` + Rust fixtures only | PASS |
| `scripts/check-thin-swift-boundary.py` (`enum InspectorMode` negative) | PASS |
| `scripts/check-host-product-fixtures.py` | PASS |

M001 production shell remains one Workspace / one Tab / one Pane. Extra
split identities fail closed in Rust (`#879`). Agent/attention rows are
projected only when the host supplies activity; this run did not fabricate
them.

## Rust product contracts (no AppKit)

Already landed on this branch: `#879` Workspace/Tab/Pane, `#880` chrome,
`#881` composer/Blocks, `#861` Flow/Raw/TUI, `#882` recovery, `#740` theme.
Busy/stale composer and inspector identity fencing are covered by
`seyal-client` lib tests.

## Performance

`--renderer-benchmark` is restored on the new host (`RendererValidation.runBenchmark`).
Local Debug diagnostic (`SEYAL_REQUIRE_DISPLAY_LINK_BENCHMARK` unset, so
display-link samples are `PLATFORM_LIMITED` — not Foundation CI proof):

```text
preparation p50_ns=32000 p95_ns=39750
prepared_to_command_commit p50_ns=12333 p95_ns=21542
command_commit_to_gpu_completion_proxy p50_ns=396792 p95_ns=1113000
display_link_samples=0
unicode warm_hit preparation_ns=719333 process_rss_bytes=50757632
performance_claim=false
```

Do not treat Foundation `SEYAL_REQUIRE_DISPLAY_LINK_BENCHMARK=0` CI as headed
proof. Comparison to `docs/evidence/m001-pass10-headed-display-link.md`
(`a012ab0`, 120 display-link samples) remains a headed presentation-proxy
measurement, not a physical scanout claim.

## Future hosts

Windows/Linux adapters can consume the same `seyal_app_*` / `ApplicationRoot`
contracts. No portable product reducer remains in Swift.
