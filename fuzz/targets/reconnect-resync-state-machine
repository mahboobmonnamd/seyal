#!/usr/bin/env bash
set -euo pipefail

input="${1:?retained fuzz seed path is required}"
SEYAL_FUZZ_INPUT="$input" cargo test --quiet -p seyal-runtime --test fuzz_smoke reconnect_resync_state_machine_seed -- --ignored --exact
